//! `dohee-cli` is the binary entrypoint for the `dohee` executable.
//! It handles CLI arguments (via `clap`), initializes the required configurations, spins up selected interfaces (CLI or TUI), and drives the session.

fn main() {
    println!("Hello from Dohee CLI!");
}
